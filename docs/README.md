# md2cd


## Generating all course descriptions


Combine these two options: 
```sh
md2cd -rf tests/fixtures/recursive-generation
```

This is the same as: 
```sh
md2cd -r -f tests/fixtures/recursive-generation
```

Or:
```sh
md2cd --recursive --flat tests/fixtures/recursive-generation
```


